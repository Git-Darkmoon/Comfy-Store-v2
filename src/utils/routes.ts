export enum ROUTES_NAME {
  HOME = "Home",
  ABOUT = "About",
  PRODUCTS = "Products",
  CART = "Cart",
  CHECKOUT = "Checkout",
  ORDERS = "Orders",
  USER = "User Profile Account",
}

export enum ROUTES_PATH {
  HOME = "/",
  ABOUT = "/about",
  PRODUCTS = "/products",
  CART = "/cart",
  CHECKOUT = "/checkout",
  ORDERS = "/orders",
  USER = "/account",
}
