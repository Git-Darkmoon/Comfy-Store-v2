export enum ROUTES_NAME {
  HOME = "Home",
  ABOUT = "About",
  PRODUCTS = "Products",
  CART = "Cart",
  CHECKOUT = "Checkout",
  ORDERS = "Orders",
  LOGIN = "Sign In",
  REGISTER = "Register",
}

export enum ROUTES_PATH {
  HOME = "/",
  ABOUT = "/about",
  PRODUCTS = "/products",
  CART = "/cart",
  CHECKOUT = "/checkout",
  ORDERS = "/orders",
  LOGIN = "/login",
  REGISTER = "/register",
}
